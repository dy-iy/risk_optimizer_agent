# Workflow v2

This directory preserves the previous workflow experiment that used to live under `backups/workflow_20260715_before_revert`.

Status: kept for reference. It was not promoted as the main workflow because the observed effect was not good enough.

Contents:

- `orchestrator.py`, `analyzer_llm.py`, `patcher_llm_v2.py`: v2 workflow code snapshot
- `tools/`, `prompts/`, `tests/`: supporting v2 modules and tests
- `versions/`: reserved for reconstructed or replayed v2 rule iterations
- `experiments/`: v2 experiment notes and run files
- `git_head_workflow.tar`: archived git-head workflow snapshot

Do not treat this as a generic backup directory. It is a named workflow version for comparison and reference.
