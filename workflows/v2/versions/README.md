# Workflow v2 Versions

The preserved v2 workflow snapshot does not currently include a complete promoted `versions/vN` iteration set.

Keep any future reconstructed or replayed v2 rule iterations here so v2 code, experiments, and version artifacts stay together.
